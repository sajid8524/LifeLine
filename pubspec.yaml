name: lifesaver_dtn_nearby_poc
description: LifeSaver DTN offline-first disaster communication MVP.
publish_to: "none"
version: 0.2.0+2

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.6.0
  connectivity_plus: ^7.2.0
  firebase_core: ^4.11.0
  flutter_riverpod: ^2.6.1
  geolocator: ^14.0.3
  google_generative_ai: ^0.4.7
  google_maps_flutter: ^2.17.1
  image_picker: ^1.2.3
  nearby_connections: ^4.3.0
  path: ^1.9.1
  permission_handler: ^12.0.3
  share_plus: ^13.2.0
  sqflite: ^2.4.3
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
